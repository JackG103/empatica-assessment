int plasticAlumLedPin= 8;
int glassLedPin = 9;
int paperLedPin = 10;
int organicLedPin = 11;
int undiffLedPin = 12;

void setup() {
  // put your setup code here, to run once:
  pinMode(plasticAlumLedPin, OUTPUT);
  pinMode(glassLedPin, OUTPUT);
  pinMode(paperLedPin, OUTPUT);
  pinMode(organicLedPin, OUTPUT);
  pinMode(undiffLedPin, OUTPUT); 
}

void loop() {
}
